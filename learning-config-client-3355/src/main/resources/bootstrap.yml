# application.yml是用户级的资源配置项
# bootstrap.xml是系统级的，优先级更高

spring:
  cloud:
    config:
      name: learning-config-client
      profile: dev
      label: master
      uri: http://config-3344.com:3344    # 此处必须要用端口映射